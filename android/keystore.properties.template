# Keystore properties for release signing
# IMPORTANT:
# 1. Copy this file to "keystore.properties" (remove .template)
# 2. Fill in your actual values
# 3. NEVER commit keystore.properties to git (it's in .gitignore)

storePassword=YOUR_KEYSTORE_PASSWORD_HERE
keyPassword=YOUR_KEY_PASSWORD_HERE
keyAlias=tetris-release
storeFile=tetris-release-key.jks
